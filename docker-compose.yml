services:
  postgres:
    image: postgres:15-alpine
    container_name: todo_postgres # TODO: вынести в .env
    environment:
      POSTGRES_DB: todo_db # TODO: вынести в .env
      POSTGRES_USER: todo_db_user # TODO: вынести в .env
      POSTGRES_PASSWORD: todo_db_pass # TODO: вынести в .env

    ports:
      - "5433:5432" # TODO: вынести в .env

    volumes:
      - db_data:/var/lib/postgresql/data
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql

    # healthcheck:
    #   test: ["CMD-SHELL", "pg_isready -U todo_db_user -d todo_db"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 5

volumes:
  db_data:
